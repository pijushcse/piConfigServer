spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D://micro-poc/Example/Spring-Cloud-Config/config-repo
        
server:
  port: 8888
  
security:
   user:
     name: debnatp
     password: picloud   